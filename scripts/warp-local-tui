#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ARGS=("$@")

if [[ " ${ARGS[*]} " != *" --profile "* ]]; then
    ARGS=(--profile debug "${ARGS[@]}")
fi

exec "$SCRIPT_DIR/warp-local" --tui "${ARGS[@]}"
